/*
函数作用：求3个数的最大值
参数：
参数一；其中一个参数
参数二：另外一个参数
参数三：第三个参数
*/

int Max_3(int x,int y,int z)
{
	int max_3,m,max;
	max_3 = x;
	if(y>z)
	{
		m = y;
	}
	else
	{
		m = z;
	}
	if(max_3 > m)
	{
		max = max_3;
	}
	else
	{
		max = m;
	}
	return max;
}
