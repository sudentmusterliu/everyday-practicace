/*
函数作用：判断一个数是否是素数
参数：
参数一；一个参数
*/


#include<math.h>
int Isprime(int x)
{
	int flag,i,div;

	flag=1;

	for(i=2;i<=sqrt(x);i++)
	{
		div = x%i;
		if(div == 0)
		{
			flag=0;
			break;
		}
	}
	return flag;
}
